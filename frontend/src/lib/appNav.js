/** App routes — single source for navigation links. */
export const APP_ROUTES = {
  home: "/",
  live: "/live",
  liveMonitor: "/live/monitor",
  history: "/history",
  settings: "/settings",
  /** Aliases so the ported live header can keep its QA link names. */
  files: "/history",
  audit: "/",
};
